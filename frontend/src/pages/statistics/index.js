import { Typography } from "@mui/material";

const Statistics = () => {
	return <Typography variant="h5" sx={{position:"absolute", top:"50%", left:"50%", transform:"translate(-50%,-50%)", fontWeight:"bold", color:"#0a4b53"}}>In progress...</Typography>;
};

export default Statistics;
